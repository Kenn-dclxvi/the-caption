# GitHub Workflows

CI/CD workflow files are placed here.

- New workflows should avoid broad path triggers.
- Keep checks scoped to changed areas when possible.
- `docs-canonical-command-guard.yml`: blocks legacy `python tools/...` command examples in docs.
