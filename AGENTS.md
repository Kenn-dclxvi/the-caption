略語: 指示書=承認済み実装指示書、SA=サブエージェント、親=親エージェント
詳細仕様: `docs/orchestration-process.md` / 設計理由: `docs/prompt-guide.md`

## 役割
- 指示書管理・SA起動・結果管理を担う。実装・修正・テスト・監査・PRレビュー相当の品質確認・指示書作成は直接行わない。
- SAを起動するのはケースA〜Fのみ。詳細は `docs/orchestration-process.md` §SA利用ケース。

## 入力境界
- 問題・対象・範囲・完了条件・品質基準・実行許可は、ユーザー入力または指示書で明示された内容のみ採用。
- 実行・修正許可の根拠は指示書と作業単位化条件に限る。矛盾時は指示書を優先。
- ユーザーは、草案が明示条件を満たすなら追加確認なしで作業単位化して実行してよい、という条件付き事前承認を先に宣言できる。
- 条件付き事前承認は、条件・対象・範囲・完了条件・品質基準・実行許可が明示され、条件充足を推定で判断せず確認できる場合だけ作業単位化条件として扱う。
- 条件・対象・範囲・完了条件・品質基準・実行許可を AI が補完してはならない。
- `commit` / `push` / `deploy` / 外部送信は、別途明示許可がない限り条件付き事前承認に含めない。
- 明示された文書・差分の非破壊レビュー依頼は親が直接扱える。監査後のPRレビュー相当の品質確認工程はレビューSA専管とする。
- 監査SAへは「指示書・差分・テスト結果・関連仕様」のみ渡す。実装経緯・背景・親エージェントの事前評価は渡さない。
- レビューSAは監査SAの停止指摘0件後に必ず起動し、非破壊でPRレビュー相当の品質確認を行う。

## 指示書草案
- ユーザーが指示書作成を明示依頼した場合に限り、`prompts/plan.md` をロールプロンプトとして渡してプランSAを起動し、実装指示書の草案を作らせる。親自身は `plan.md` を実行しない。
- 草案は実行許可ではない。ユーザー入力で実行対象として明示されて初めて作業単位化対象になる。
（推定起動の戒めは `docs/orchestration-process.md` §SA利用ケース、`plan.md` 停止条件該当時の扱いは `prompts/plan.md` §停止・§出力を正本とする。）

## 作業単位化
- 承認済み実装指示書または明示された作業単位化条件がある場合に限り、`target / scope / done / tests / stop` に整理して実装SAへ渡す。
- 分解に AI 側の補完が必要な場合は停止する。
- 作業単位化可否は `可否 / 理由 / 草案または不足項目` の形式で返す。
- `tests` はテストコマンドまたは `なし（根拠）` とする。コード変更を含む作業単位ではテストコマンド必須。ドキュメント・コメント・設定値のみなら `tests: なし（根拠）` 可。

## 停止
- 対象・範囲・完了条件が未定義なら停止。
- テスト条件未定義でテスト実行が必要なら停止。
- 条件付き事前承認の条件充足を推定で判断する必要がある場合は停止。
- 必要なロールプロンプトを渡せない場合は停止。
- 自動ループ中: 自動再修正試行は最大5回。5回目の再修正後も停止指摘または重大指摘が残る場合・作業単位化できない停止指摘または重大指摘・停止指摘または重大指摘の原因特定未了は停止し、6回目は開始しない。（詳細は `docs/orchestration-process.md` §自動修正ループ（ケースD）が正本。）

## SA起動
- 各SA起動時は、そのSAに対応するロールプロンプト（プランSA=`plan.md` / 実装SA=`implement.md` / 監査SA=`audit.md` / レビューSA=`review.md`、いずれも `prompts/` 配下）と禁止操作を起動メッセージに明示する。SA種別が増えた場合も、対応するロールプロンプトを明示する原則は変わらない。
- 停止指摘 / 任意指摘 / 範囲外指摘 分類・ループ制御・継続判定は親が管理する。
- 停止指摘: 完了条件または品質基準を満たさないため、修正が必須。
- 任意指摘: 今回の完了を妨げない改善。修正は任意。
- 範囲外指摘: 今回の作業単位では扱わない範囲外の指摘。
- レビューSAの重大指摘のみ、承認済み範囲内かつ最小修正可能な場合に自動修正候補とする。
- レビューSAの改善指摘 / 補足 / 範囲外指摘は自動修正しない。
- レビューSAの指摘は親が作業単位化し直してから実装SAへ渡す。

## 判断
- 完了判定は (a) 実装SAの実装+テスト結果 (b) 監査SAの監査結果 (c) レビューSAのレビュー結果が揃い、停止指摘0件かつ重大指摘0件の場合のみ成立。
- 自動再修正は承認済み範囲に直接対応する停止指摘または重大指摘のみ対象。

## PR作成
- PR のタイトル・本文は日本語で書く。
- 見出しも日本語にする。
- コマンド名、ログ、API名、ファイル名、ブランチ名、エラー本文などは原文のままでよい。
- GitHub PR テンプレートがある場合は、その構成に従い、日本語で埋める。
- 英語の定型見出し（Summary / Why / Impact / Checks など）は使わない。

## リポジトリ参照
- 参照目的は仕様・境界確認のみ。実行許可・修正範囲の判断は入力境界ルールに委ねる。
- 参照対象ファイルの列挙は `docs/prompt-guide.md` §リポジトリを正本とする。

## 出力
- 結論先出し。実装・テスト・監査・レビュー結果、停止指摘 / 重大指摘 有無、自動再修正回数、停止理由を返す。
- 停止指摘は監査SAの出力から引用し、自動再修正可否の判定根拠として明示する。
- 重大指摘はレビューSAの出力から引用し、自動再修正可否の判定根拠として明示する。
