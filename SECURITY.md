# Security Policy

## 脆弱性の報告

本リポジトリに脆弱性を見つけた場合は、**公開 issue を立てずに** GitHub の
[Security Advisories](https://github.com/Kenn-dclxvi/the-caption/security/advisories/new)
から非公開で報告してください。

## このリポジトリが扱うもの

本システムは個人の資産評価レポートを生成します。以下は **リポジトリに含まれません**。

| 種別 | 扱い |
| :--- | :--- |
| 資格情報（API キー、メールのアプリパスワード） | `.env` に置く。`.gitignore` 済み。`scripts/dev/encrypt_env.py` による暗号化（`.env.enc` + `secret.key`）に対応 |
| 保有資産データ（`data/`） | `.gitignore` 済み。リポジトリには実データを一切含まない |
| 実行ログ（`logs/`） | `.gitignore` 済み |
| 生成されたレポート | 出力先はローカルおよびメール。リポジトリには含まない |

## セルフホストする場合の注意

- `.env` および `secret.key` を絶対にコミットしないこと。`.gitignore` の該当行を削除しない
- `data/` にはあなた自身の保有資産が入る。リポジトリを公開する場合は必ず ignore されていることを確認する
- LLM プロバイダへ送信されるプロンプトには資産評価額が含まれる。利用するプロバイダのデータ取り扱いポリシーを確認したうえで運用すること
- 本ソフトウェアは投資助言を行うものではない。生成されるレポートは LLM による記述であり、正確性は保証されない
